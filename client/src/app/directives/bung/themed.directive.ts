import { Directive, ElementRef } from "@angular/core"
import { BungThemeService } from "../../services/bung/theme.service"

@Directive({
    selector: "[bungThemed]",
    standalone: false
})
export class BungThemedDirective {

    
}
